import Link from 'next/link';

const Logo: React.FC = () => {
    return (
        <Link href="/" className="text-2xl font-bold">
            Read With Me
        </Link>
    );
};

export default Logo;
